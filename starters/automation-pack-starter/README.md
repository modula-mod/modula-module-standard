# Automation Pack Starter

Reference starter for an `automation-pack` package.

Use `modula create automation-pack <name>` for the live scaffold.
This folder exists as a readable canonical example inside the module standard repo.
