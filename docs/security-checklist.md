# Security Checklist

Before publishing or submitting a package:

- verify the GitHub repo is the canonical source
- verify the release artifact URL matches the repo and version
- verify permissions are explicit and minimal
- verify automation templates declare confirmation boundaries when needed
- verify public surfaces do not leak private identity state
- verify support and source links are present
- verify package metadata identifies the publisher clearly
