# Unsafe Capability Denial Fixture

This is a negative Builder validation fixture. It intentionally requests mutating capabilities and must fail the `sandbox_capability` hard gate.

Do not install, publish, or place this package in Marketplace.
