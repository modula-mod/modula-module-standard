# Module Search

Modules declare searchable records and projections.

Search definitions include:

- target record;
- indexed fields;
- ranking hints;
- sensitive fields;
- visibility;
- declarative projection handler.

Standard 1.0 does not allow arbitrary projection runtime code inside the manifest.
