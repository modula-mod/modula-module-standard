# Vault Notes Reference Example

Vault Notes is the first expected reference implementation after Standard 1.0.

This Block 5 repository provides the Standard 1.0 fixture in `@modula/module-fixtures`. The module implementation remains in its own repository and is not built here.
