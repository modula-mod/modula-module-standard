{
  "name": "modula-module-standard-repo",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.23.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm build && node tests/module-standard.test.mjs",
    "verify": "pnpm test && node tests/security.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "typescript": "~5.9.2"
  }
}
